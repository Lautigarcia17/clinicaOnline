import { ColourStateDirective } from './colour-state.directive';

describe('ColourStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ColourStateDirective();
    expect(directive).toBeTruthy();
  });
});
