import { TooltipTittleDirective } from './tooltip-tittle.directive';

describe('TooltipTittleDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipTittleDirective();
    expect(directive).toBeTruthy();
  });
});
