import { FormatDoubleZeroPipe } from './format-double-zero.pipe';

describe('FormatDoubleZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDoubleZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
