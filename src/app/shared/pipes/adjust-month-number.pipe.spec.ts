import { AdjustMonthNumberPipe } from './adjust-month-number.pipe';

describe('AdjustMonthNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AdjustMonthNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
